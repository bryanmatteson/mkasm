// Package conformance contains the repository's independent, corpus-scale
// verification suites. These tests deliberately live outside implementation
// packages so unit consistency checks cannot be mistaken for external proof.
package conformance
