// Code generated by mkasm. DO NOT EDIT.

package registry

// Instruction is a compact catalog entry for one encoding.
type Instruction struct {
	EncodingID string
	Mnemonic   string
	Class      string
	BitPattern string
	FixedWord  uint32 // fixed bits only; 0 if unknown
	HasFixed   bool
	IFormFile  string
	Asm        string
	AliasOf    string // canonical EncodingID when this entry is an alias
}

// InstructionCount is the number of known encodings at generation time.
const InstructionCount = {{.Count}}

// All is the complete encoding catalog.
var All = []Instruction{
{{range .Entries}}	{ {{printf "%q" .EncodingID}}, {{printf "%q" .Mnemonic}}, {{printf "%q" .Class}}, {{printf "%q" .Pattern}}, {{printf "0x%08X" .FixedWord}}, {{.HasFixed}}, {{printf "%q" .IFormFile}}, {{printf "%q" .Asm}}, {{printf "%q" .AliasOf}} },
{{end}}}

// ByEncodingID indexes All by EncodingID.
var ByEncodingID = map[string]*Instruction{}

// ByMnemonic indexes All by mnemonic (may be multi-valued).
var ByMnemonic = map[string][]*Instruction{}

func init() {
	for i := range All {
		inst := &All[i]
		ByEncodingID[inst.EncodingID] = inst
		ByMnemonic[inst.Mnemonic] = append(ByMnemonic[inst.Mnemonic], inst)
	}
}

// Lookup returns the catalog entry for an encoding ID.
func Lookup(encodingID string) (*Instruction, bool) {
	inst, ok := ByEncodingID[encodingID]
	return inst, ok
}
