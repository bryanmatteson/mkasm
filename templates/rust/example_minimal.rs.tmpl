// Generated by mkasm — do not edit.
//
// This generate resolved only part of the instruction set (see -max-iforms), so
// the worked example is reduced to the parts that are always present.

use {{.CrateName}}::*;

fn main() {
    let mut asm = Assembler::new();
    asm.emit(0xD503201F); // nop
    let code = asm.finish().expect("encoding failed");
    println!("{code:08X?}");
    println!("{TYPED_ENCODING_COUNT} of {ENCODING_COUNT} encodings have a typed method in this build");
}
