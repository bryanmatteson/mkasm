//! Generated by mkasm — do not edit.

#[derive(Debug, Clone, Copy)]
pub struct Instruction {
    pub encoding_id: &'static str,
    pub mnemonic: &'static str,
    pub class: &'static str,
    pub bit_pattern: &'static str,
    pub fixed_word: u32,
    pub has_fixed: bool,
    pub iform_file: &'static str,
    pub asm: &'static str,
    pub alias_of: &'static str,
}

{{.AllTable}}

pub const INSTRUCTION_COUNT: usize = ALL.len();

/// Lookup by EncodingID (binary search on sorted ALL).
pub fn lookup(id: &str) -> Option<&'static Instruction> {
    ALL.binary_search_by(|e| e.encoding_id.cmp(id))
        .ok()
        .map(|i| &ALL[i])
}
